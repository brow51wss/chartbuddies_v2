# SENTINEL VERIFICATION REPORT

- **Scope:** verification of fixes claimed against `/Users/marlonsarmiento/Desktop/chartbuddies_v2/audits/2026-09-02-auth-touched.md`
- **Project fingerprint:** `ca782299d76e8e35`
- **Source report fingerprint:** `ca782299d76e8e35`
- **Fingerprint match:** ✓ match
- **Scanned:** 2026-09-02T14:42:03
- **Repo path:** `/Users/marlonsarmiento/Desktop/chartbuddies_v2`
- **Git HEAD:** 80ee61fa (branch: `new-layout`) · ⚠️ uncommitted changes present
- **Prior findings parsed:** 112
- **In scope this run:** 4
- **Regressions flagged:** 5
- **Generated by:** Sentinel MCP (deterministic Pass 3)

---

## Per-finding evidence (deterministic)

The calling LLM should review each finding's evidence below, optionally read the
relevant files for context, and assign a final verdict by editing this file.

### [CRITICAL] Unauthenticated staff roster leaks e-signature data with no rate limiting — enables account enumeration and credential stuffing against staff sign-in [Cowork finding]
- **File:** `pages/api/staff/users.ts` (lines 16-28), chained with `pages/auth/staff-login.tsx` (lines 72-83, 99) and `pages/api/auth/staff-signin.ts`
- **Rule:** ``cowork.auth_chain_phi_adjacent` (Cowork auditor finding — not from the deterministic scanner)`
- **Original instruction:** This route has to stay reachable without authentication by product design — nurses pick their name tile *before* entering a password, so it can't simply be gated behind a login check without redesigning the staff-login flow. Given that, fix what's actually controllable: (1) trim the `.select()` to only the columns the UI reads — `id, full_name, first_name, last_name, staff_initials_text, role` — and drop `staff_initials` (an S3 image path) and especially `staff_signature` (the typed legal name used to e-sign MAR/PHI records, currently exposed to anyone who has a `hospital_id`); (2) add rate limiting to this route AND to `pages/api/auth/staff-signin.ts` (e.g. `@upstash/ratelimit`, keyed by IP + `hospital_id` here and IP + `user_profile_id` there) — right now an attacker who obtains a `hospital_id` can pull every nurse's `id` from this route with zero throttling, then brute-force 8-character passwords against staff-signin.ts with zero throttling; (3) confirm in the Supabase dashboard whether `hospitals` actually has a public/anon SELECT policy — no migration file in `supabase-migrations/` defines one, so the code comment asserting it exists is unverified, and the facility-name search on `staff-login.tsx` is how an outsider gets a `hospital_id` in the first place.

**Deterministic evidence:**
- `file_exists` → `False`
- ⚠️ Reported file does not exist — may have been renamed or deleted.

**Verdict: PARTIAL.** Confirmed by direct source read, not by the "excerpt gone" signal above (per instructions, that signal does not mean "now requires login" and this route correctly still doesn't — it can't, by design, since nurses pick a tile before entering a password). What's actually done: (1) `.select()` in `pages/api/staff/users.ts` is trimmed to `id, full_name, first_name, last_name, staff_initials_text, role` — `staff_signature` and `staff_initials` are gone, confirmed in source; (2) rate limiting added to both this route (30 req/60s per IP) and `staff-signin.ts` (20/15min per IP, 5/15min per `user_profile_id`) via a new `lib/rate-limit.ts`; (3) the `hospitals` anon-SELECT question is resolved and turns out worse than suspected — migration `074_revoke_anon_hospitals_select.sql`'s own comment confirms it was diagnosed live ("anon REST GET /hospitals returned rows (HTTP 206) and could read `invite_code`"), and the app no longer depends on it (`staff-login.tsx` now calls the new `/api/staff/facilities` route, confirmed by source). Not a full PASS for two reasons, both already self-disclosed in this report's "Findings NOT verified" section: (a) `lib/rate-limit.ts` is an in-memory `Map` — it will not share counters across concurrent Amplify Lambda instances, so the 20-attempt/15-min ceiling is per-instance, not global, under real concurrent load; (b) migration 074 is correctly *not yet* applied (must wait for the Amplify deploy it depends on), but that means the underlying anon `SELECT` grant on `hospitals` — including `invite_code` — is still live at the database level today, reachable by anyone calling Supabase's REST API directly with the public anon key, independent of what this app's own frontend calls. Real, substantial progress; not yet fully closed.

### [HIGH] Supabase service role key used in route with no auth check
- **File:** `pages/api/debug-env.ts`
- **Rule:** `auth.service_role_in_unauthed_route`
- **Original instruction:** The file `pages/api/debug-env.ts` uses the Supabase service role key, which bypasses all RLS policies, but has no Authorization header check or session verification. Any unauthenticated caller can trigger this route with full database access. Either add a Bearer token auth check and verify the caller's role, or replace the service role key with the anon key if elevated privileges are not needed.

**Deterministic evidence:**
- `file_exists` → `True`
- `original_excerpt_still_present` → `False`
- ✓ Original excerpt no longer in file.

**Verdict: PASS.** Confirmed by direct source read. The route now requires a `Bearer` token that resolves via `auth.getUser()` to an active `user_profiles` row with `role === 'superadmin'`, and returns a uniform `404` (not `401`/`403`) on every failure path — missing token, invalid token, inactive account, non-superadmin role, or missing server config. Using 404 uniformly avoids leaking the route's existence to unauthenticated probing, which is a better implementation than a bare auth check would have been. Fully resolves the original finding.

### [HIGH] Supabase service role key used in route with no auth check
- **File:** `pages/api/auth/staff-signin.ts`
- **Rule:** `auth.service_role_in_unauthed_route`
- **Original instruction:** The file `pages/api/auth/staff-signin.ts` uses the Supabase service role key, which bypasses all RLS policies, but has no Authorization header check or session verification. Any unauthenticated caller can trigger this route with full database access. Either add a Bearer token auth check and verify the caller's role, or replace the service role key with the anon key if elevated privileges are not needed.

**Deterministic evidence:**
- `file_exists` → `True`
- `original_excerpt_still_present` → `False`
- ✓ Original excerpt no longer in file.

**Verdict: PARTIAL.** Reaffirming the Pass 2 call: "no auth check" was never the right fix for a login route, so this specific finding was a false-positive framing from the start (do not treat it as needing a login gate). What actually addresses the real risk — brute force — is implemented: `staff-signin.ts` now rate-limits by IP (20/15min) and by `user_profile_id` (5/15min), confirmed in source. Marked PARTIAL rather than PASS for the same reason as the CRITICAL finding above: the rate limiter is in-memory and per-Lambda-instance, not distributed, so it meaningfully raises the bar over the prior zero-throttling baseline without being the full production-grade fix yet.

### [HIGH] Supabase service role key used in route with no auth check
- **File:** `pages/api/staff/users.ts`
- **Rule:** `auth.service_role_in_unauthed_route`
- **Original instruction:** The file `pages/api/staff/users.ts` uses the Supabase service role key, which bypasses all RLS policies, but has no Authorization header check or session verification. Any unauthenticated caller can trigger this route with full database access. Either add a Bearer token auth check and verify the caller's role, or replace the service role key with the anon key if elevated privileges are not needed.

**Deterministic evidence:**
- `file_exists` → `True`
- `original_excerpt_still_present` → `False`
- ✓ Original excerpt no longer in file.

**Verdict: PARTIAL.** Same underlying route and same fix as the CRITICAL finding above (kept intentionally reachable pre-auth by product design — "excerpt gone" here does not mean auth was added, and it should not be added without redesigning the staff-login flow). See that verdict for full detail: field trim confirmed complete, rate limiting confirmed added, distributed-store caveat still applies.


---

## Regression scan

Prior findings re-checked across the entire codebase to detect reintroduction of
their original anti-patterns elsewhere.

### Regression — original finding: Use of `dangerouslySetInnerHTML` (was CRITICAL)
- **Type:** excerpt_reappeared_elsewhere (xss.dangerously_set_inner_html)
- **Hits:**
  - `pages/patients/[id]/mar/[marId].tsx:3614`

### Regression — original finding: Sensitive field `email` appears in URL string (was HIGH)
- **Type:** excerpt_reappeared_elsewhere (data_exposure.sensitive_in_url)
- **Hits:**
  - `pages/onboarding.tsx:97`

### Regression — original finding: Sensitive field `email` appears in URL string (was HIGH)
- **Type:** excerpt_reappeared_elsewhere (data_exposure.sensitive_in_url)
- **Hits:**
  - `pages/profile.tsx:140`

### Regression — original finding: Hardcoded `http://` URL (insecure transport) (was HIGH)
- **Type:** excerpt_reappeared_elsewhere (communication.insecure_http)
- **Hits:**
  - `pages/auth/signup.tsx:837`

### Regression — original finding: Hardcoded `http://` URL (insecure transport) (was HIGH)
- **Type:** excerpt_reappeared_elsewhere (communication.insecure_http)
- **Hits:**
  - `pages/auth/login.tsx:264`


---

## Findings NOT verified by this run

_(awaiting Cowork auditor judgment on all verdicts below — implementing agent does not assign PASS/FAIL.)_

Runtime / dashboard checks the deterministic scan cannot prove — Cowork confirmations below, from direct source reads on 2026-09-02:
- Unauthenticated GET `/api/debug-env` returns 404 — **CONFIRMED in source.** Every failure path (`missing config`, `no token`, `getUser() failure`, `inactive`, `not superadmin`) returns the same `404`, not a distinguishing `401`/`403`. Could not execute the live HTTP request from this environment; source logic is unambiguous, so this is a source-level confirmation, not a substitute for an actual runtime hit — cheap to do as a final check before/after deploy.
- GET `/api/staff/users` no longer returns `staff_signature` / `staff_initials` — **CONFIRMED in source.** `.select('id, full_name, first_name, last_name, staff_initials_text, role')`, line 37 — both fields are gone.
- Rate limit 31st request returns 429 — **CONFIRMED as correct logic in source** (`lib/rate-limit.ts`: `rateLimit()` returns `ok:false` once `count >= limit`, `rejectTooMany()` sends 429 + `Retry-After`). The caveat already raised in this report stands and is the main open item: this is a per-process in-memory `Map`, so the 429 fires reliably within one warm Lambda instance but does **not** enforce a shared ceiling across concurrent instances — under real distributed load (which is exactly the shape of a credential-stuffing attack) the effective limit is closer to "N attempts × however many concurrent instances Amplify spins up" than a hard 20 or 30. Real mitigation, not a complete one.
- Facility search uses `/api/staff/facilities` instead of client `hospitals` SELECT — **CONFIRMED in source.** `pages/auth/staff-login.tsx:79` — `fetch(\`/api/staff/facilities?q=${encodeURIComponent(trimmed)}\`)`. No remaining `supabase.from('hospitals')` call in this file.
- Migration `074_revoke_anon_hospitals_select.sql` has **not** been applied to live Supabase — **CONFIRMED correct, not a gap.** The migration's own header comment explains why it's sequenced this way ("Diagnosed 2026-09-02: anon REST GET /hospitals returned rows (HTTP 206) and could read invite_code... Apply this in the Supabase SQL editor after that API is deployed"). Applying it before the new `/api/staff/facilities` route ships would break facility search for anyone still on old client code. Correct order: deploy app code, then apply this migration — **the risk is leaving it unapplied for long after deploy, not applying it now.** The underlying database grant this migration revokes (anon SELECT on `hospitals`, including `invite_code`) is still live today, reachable directly against Supabase's REST API with the public anon key, independent of what this app's frontend calls.
- `NODE_TLS_REJECT_UNAUTHORIZED=0` removed from `lib/rds.ts` — **CONFIRMED.** `grep -n "NODE_TLS" lib/rds.ts` returns only a comment (line 36) referencing it as historical context for why `sslmode` is now stripped from the connection URI; the actual `process.env.NODE_TLS_REJECT_UNAUTHORIZED = '0'` assignment line is gone. A stale reference exists only in compiled `.next/` build output from before the fix — not source, will clear on next build, not a concern. Repo-wide grep for the same string turns up nothing else.
- `ssl: { rejectUnauthorized: false }` on the RDS pool was intentionally left in place — **CONFIRMED**, and this is the same accepted risk from the Sep 2 Pass 2 report, not a new or expanded bypass. `lib/rds.ts:53` unchanged; the new code (`getRdsConnectionString()`, lines 31-38) strips `sslmode=` from the RDS connection URI with a comment explaining exactly why: node-pg's URI-level `sslmode` parameter was overriding the `Pool`-level `ssl` config, which is what caused `SELECT_CERT_IN_CHAIN`-style failures (`SELF_SIGNED_CERT_IN_CHAIN`, per the add-resident failure this was fixing) once the process-wide bypass was removed. This is a narrow, correct fix for a real node-pg interaction bug — it reaches the *same* end state as before (`rejectUnauthorized: false` on this one pool only), it does not reintroduce the process-wide bypass, and it is not a new TLS decision requiring a fresh audit. Confirmed: **not** a process-wide bypass.

## Pending verdict assignment

Verdicts assigned above, per-finding: 1 PASS (`debug-env.ts`), 3 PARTIAL (the `staff/users.ts` chain and its two duplicate scanner lines) — see "Per-finding evidence" section above for justification on each. No FAIL.

---

## Pending verdict assignment

**Regression scan — CONFIRMED, all 5 are carry-over, none are new introductions.** Checked each hit's file:line against the Sep 2 Pass 2 triage rather than trusting the implementing agent's own framing:
- `pages/patients/[id]/mar/[marId].tsx:3614` (`dangerouslySetInnerHTML`) — same file, same finding directly read and confirmed static print-CSS with zero user input in Pass 2 (Pending Manual Review section of `2026-09-02-auth-touched.md`). Not touched by this branch; not a release blocker per Pass 2 instructions.
- `pages/onboarding.tsx:97` and `pages/profile.tsx:140` (sensitive field `email` in URL) — both exact file:line matches to Pass 2's HIGH triage table, already confirmed false positive (word matched inside the route *path*, not a query-string value).
- `pages/auth/signup.tsx:837` and `pages/auth/login.tsx:264` (`http://` insecure transport) — both exact file:line matches to Pass 2's HIGH triage, already confirmed false positive (`xmlns` namespace on an inline `<svg>`, not a network request).

No new pattern, no new file, no severity change. These re-appear because Pass 3's regression scan re-runs the same excerpt match across the whole codebase every time, by design — it is not evidence anything regressed.

---

## Ship / no-ship (Pass 3, 2026-09-02)

**SHIP**, conditional on two commitments rather than pre-deploy blockers:

1. **Apply `074_revoke_anon_hospitals_select.sql` immediately once the Amplify deploy carrying `/api/staff/facilities` completes — not "eventually."** Until it's applied, the anon `SELECT` grant on `hospitals` (including `invite_code`) is still live at the database level and reachable by anyone calling Supabase's REST API directly with the public anon key, regardless of what this app's own frontend now calls. The app-side fix (routing through `/api/staff/facilities`) does not, by itself, close that database-level hole.
2. **Treat the in-memory rate limiter (`lib/rate-limit.ts`) as a stopgap, not the final state**, for `staff/users.ts` and `staff-signin.ts`. It's a real improvement over zero throttling and is fine to ship with, but it will not hold a hard ceiling under genuinely concurrent/distributed requests once this runs across multiple Amplify Lambda instances — the report's own "Findings NOT verified" section already flagged this. Move to a shared store (`@upstash/ratelimit` + Redis, or a Supabase-table-backed counter) as a near-term fast-follow, not a someday item, given this gates brute-force protection on staff sign-in for a HIPAA application.

Everything else from the Sep 2 must-fix list is resolved: `staff/users.ts` no longer over-fetches (`staff_signature`/`staff_initials` confirmed gone from the response), `debug-env.ts` is fully fixed (real superadmin auth check, uniform 404), and the previously-unverified `hospitals` anon policy is now not just confirmed but was worse than suspected (it exposed `invite_code`) — and is being closed via the new server-side facilities route plus a correctly-sequenced revoke migration. The 5 regression hits are stale carry-over noise, not new problems. No FAIL verdicts on this pass.

---

## Addendum — `lib/rds.ts` TLS state, re-confirmed (2026-09-02, same day)

Requested as a follow-up to the Pass 3 review above; not a new pass, no other files re-scanned.

Re-checked `lib/rds.ts` directly against the three "current state, keep this" points:
- `NODE_TLS_REJECT_UNAUTHORIZED=0` is still gone — **CONFIRMED.** `grep -n "NODE_TLS" lib/rds.ts` returns only the explanatory comment at line 36 referencing it as historical context; no live assignment. Repo-wide grep (excluding `node_modules`/`.next`) turns up nothing else.
- `sslmode` is stripped from the connection string — **CONFIRMED.** `getRdsConnectionString()` (lines 31-38) strips `sslmode=...` from the RDS URI via regex before it reaches `pg.Pool`.
- The RDS pool still uses `rejectUnauthorized: false` only — **CONFIRMED.** Line 53, unchanged, scoped to this one `Pool`'s `ssl` config — not process-wide.

This matches the root cause as described: `sslmode=require` in the RDS connection URI overrides `Pool.ssl` in this `pg` version, so once the process-wide `NODE_TLS_REJECT_UNAUTHORIZED=0` bypass was removed, the URI's own `sslmode` took over and enforced full cert verification — which is what produced `SELF_SIGNED_CERT_IN_CHAIN` against RDS's self-signed/AWS-managed cert. Stripping `sslmode` from the URI removes that override, so the pool's explicit `rejectUnauthorized: false` is what actually governs the connection again — the same accepted-risk end state as before this branch, reached through a narrow, correct fix for a real `node-pg` interaction bug, not a new TLS decision. **Not a process-wide TLS disable — confirmed.** No caregiver/staff-auth code was touched by this fix and nothing here changes the auth findings already verified above; **not a reason to re-open the caregiver/auth audit.**

**Folded into ship/no-ship: no change to the verdict.** This was already accounted for in the Ship/no-ship section above (which already states the `rejectUnauthorized: false` / `sslmode`-stripping change "reaches the same accepted-risk end state as before... not a new or process-wide bypass"); this addendum re-confirms that description against source, word for word, and finds nothing to correct. Ship/no-ship stands: **SHIP**, conditional on the two commitments already listed (apply migration 074 promptly post-deploy; treat the in-memory rate limiter as a stopgap).
